{
  "name": "runway",
  "type": "module",
  "version": "2.7.2",
  "description": "Local-first monitoring for AI provider quotas",
  "private": true,
  "scripts": {
    "web:install": "npm --prefix webapp ci",
    "web:dev": "npm --prefix webapp run dev",
    "web:build": "npm --prefix webapp run build",
    "web:test": "npm --prefix webapp test"
  },
  "license": "AGPL-3.0-only"
}
